node_modules
.next
out
build
.DS_Store
*.pem

.env
.env*.local

.vercel
next-env.d.ts
*.tsbuildinfo

supabase/.branches
supabase/.temp

# Playwright: traces, screenshots and failure context. Not source, and large.
test-results
playwright-report

# Agent skill bundles, vendored by `clerk init` and `npx skills add`. Tooling,
# not source: they are large, and they carry their own template projects whose
# TypeScript would otherwise be typechecked as part of this app. The lockfile
# that pins them goes with them.
agent/
.agents/
.claude/
skills-lock.json

.impeccable
.env*
!.env.example
